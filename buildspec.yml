version: 0.2
phases:
  install:
    runtime-versions:
      jave: corrretto 11
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - httpd install
  post_build:
    commands:
      - echo Build completed on `date`
